import Vue from 'vue';
import { IStyle } from './inteface';
declare const _default: {
    get: (property: string, self: Vue, defaultValue: number) => number;
    set: (style: IStyle) => number;
};
export default _default;
